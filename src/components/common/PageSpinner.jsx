import React from "react";
import { ReactComponent as Spinner } from "../../assets/icons/spinner.svg";

const PageSpinner = () => {
  return <Spinner style={{ margin: "auto" }} width={200} height={200}></Spinner>;
};

export default PageSpinner;
